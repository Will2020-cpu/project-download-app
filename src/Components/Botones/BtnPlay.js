import {makeStyles} from '@material-ui/core/styles'

const btnPlayStyles = makeStyles ((theme)=>({
    root:{
        color:'#f5c518',
        marginLeft:'10px',
        fontSize:'1rem'
    }
}))

export default  btnPlayStyles